static int run_prog0(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 1;
x += 14;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 8;
y *= x;
z += y;
    return z;
}

static int run_prog1(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 1;
x += 15;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 11;
y *= x;
z += y;
    return z;
}

static int run_prog2(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 1;
x += 13;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 2;
y *= x;
z += y;
    return z;
}

static int run_prog3(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 26;
x += -10;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 11;
y *= x;
z += y;
    return z;
}

static int run_prog4(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 1;
x += 14;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 1;
y *= x;
z += y;
    return z;
}

static int run_prog5(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 26;
x += -3;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 5;
y *= x;
z += y;
    return z;
}

static int run_prog6(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 26;
x += -14;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 10;
y *= x;
z += y;
    return z;
}

static int run_prog7(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 1;
x += 12;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 6;
y *= x;
z += y;
    return z;
}

static int run_prog8(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 1;
x += 14;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 1;
y *= x;
z += y;
    return z;
}

static int run_prog9(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 1;
x += 12;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 11;
y *= x;
z += y;
    return z;
}

static int run_prog10(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 26;
x += -6;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 9;
y *= x;
z += y;
    return z;
}

static int run_prog11(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 26;
x += -6;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 14;
y *= x;
z += y;
    return z;
}

static int run_prog12(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 26;
x += -2;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 11;
y *= x;
z += y;
    return z;
}

static int run_prog13(int z, int d) {
    int x = 0;
    int y = 0;
    int w = 0;

w = d;
x *= 0;
x += z;
x %= 26;
z /= 26;
x += -9;
x = x == w ? 1 : 0;
x = x == 0 ? 1 : 0;
y *= 0;
y += 25;
y *= x;
y += 1;
z *= y;
y *= 0;
y += w;
y += 2;
y *= x;
z += y;
    return z;
}

